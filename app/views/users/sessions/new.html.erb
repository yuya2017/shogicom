<div class="container-fluid">
  <div class="row">
    <%= render "sidebar" %>
		<main class="col-md-9 px-4 ml-sm-auto header_next">
      <h1 class="heading">ログイン</h1>
      <p class="login_title">登録がお済みのユーザー</p>
      <div class="login_group">
        <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <div class ="login_form">
          <div class="field form_group login_content">
            <%= f.label :email, 'Eメールアドレス*' %>
            <%= f.email_field :email, autofocus: true, autocomplete: "email", class: 'form-control'  %>
          </div>
          <div class="field form_group login_content">
            <%= f.label :password, 'パスワード' %>
            <%= f.password_field :password, autocomplete: "current-password", class: 'form-control'  %>
          </div>
          <% if devise_mapping.rememberable? %>
          <div class="field login_content">
            <%= f.check_box :remember_me %>
            <%= f.label :remember_me %>
          </div>
          <% end %>
        </div>
        <div class="actions buttonContainer">
          <%= f.submit "ログイン", class: "btn btn-primary buttonConfirm"%>
        </div>
        <% end %>
      </div>
      <%= render "users/shared/links" %>
      <p class="login_title notlogin_text">まだご登録がお済みでないユーザー</p>
      <div>
        <p>登録していただくと、投稿機能、マイページなどとて便利にお使いいただけます。</p>
      </div>
      <div class="actions guestContainer">
        <%= link_to "新規登録", new_registration_path(resource_name), class: "btn buttonConfirm btn-info" %>
      </div>
    </main>
  </div>
</div>
